# گزارش نهایی — بازطراحی ماژول بازار (سه Universe) + اصلاحیه تحلیل Pendle

تاریخ: ۱۱ اوت ۲۰۲۶ · نسخه: دارینو PWA

---

## ۱) بازطراحی کامل ماژول بازار

صفحه بازار (`/` و `/market`) اکنون **سه Universe کاملاً مستقل** دارد که هر کدام Data Source،
Data Model، محاسبات و جدول مخصوص خودش را دارد:

```
┌──────────────────────────────┐
│  TOKENIZED RWA (CoinGecko)   │
│  CRYPTOCURRENCY (CoinGecko)  │
│  TRADITIONAL FINANCE (AV)    │
└──────────────────────────────┘
```

### ۱-۱) Tokenized Assets / RWA

- **منبع**: CoinGecko — دسته‌های رسمی `tokenized-*` (بدون هیچ لیست Hardcode).
- **طبقه‌بندی پویا** بر اساس Metadata واقعی Provider:
  سهام توکن‌ایز / کامودیتی / اوراق خزانه / اوراق قرضه / ETF/ETP / صندوق پول بازار (MMF) /
  اعتبار خصوصی / ملک / سایر — هر دسته با categoryId رسمی کوین‌گکو (مثل
  `tokenized-stock`، `tokenized-treasuries`، `tokenized-money-market-fund-mmfs`، …).
- **جدول دقیقاً مطابق مشخصات**:
  `# | Name | Price (پایه) | Tokenized Price | 24h % | 7d % | Market Cap (پایه) | Tokenized Mcap | Tokenized 24h Vol | Last 7 Days`
- **Price و Tokenized Price دو ستون مستقل‌اند**؛ قیمت پایه فقط وقتی نمایش داده می‌شود که
  Provider داده واقعی ارائه کند (مثلاً دسته `tokenized-btc` با قیمت زنده بیت‌کوین) — در غیر
  این صورت **N/A** (هرگز حدس نمی‌زنیم).
- **لوگوی واقعی Provider** (آدرس تصویر CoinGecko) — بدون لوگوی دستی/ایموجی.
- **اسپارکلاین ۷ روزه واقعی** (`sparkline_in_7d` از API — بدون داده → «بدون داده» مشخص).
- چرخه همگام‌سازی: ۱۰ دقیقه؛ ادغام با داده قبلی (هرگز پاک نمی‌شود)؛ پیش‌رونده هنگام محدودیت نرخ.

### ۱-۲) Cryptocurrency

- **Top 200 پویا بر اساس مارکت‌کپ** (رتبه هر رفرش به‌روز می‌شود — ۲ دقیقه).
- **جدول دقیقاً مطابق مشخصات**:
  `# | Coin | Price | 1h | 24h | 7d | 30d | 24h Volume | Market Cap | FDV | MCap/FDV | Last 7 Days`
- **MCap/FDV** در موتور محاسبه می‌شود؛ اگر FDV صفر/null باشد → **N/A** (قانون ۲۱).
- لوگو + نام + نماد از داده زنده؛ نام فارسی از دیکشنری (فقط برچسب نمایش — نه داده بازار).

### ۱-۳) Traditional Finance / TradFi

- **منبع**: Alpha Vantage — کلید API **فقط در محیط سرور** (متغیر محیطی
  `ALPHAVANTAGE_API_KEY`)؛ کلید هرگز به Client نمی‌رسد (قانون ۲۳).
- کاتالوگ نماد مرجع (۱۵ نماد: سهام/ETF/کامودیتی/اوراق) — **فقط هویت، بدون هیچ داده قیمتی**؛
  همه اعداد از Provider.
- **سهمیه‌بندی روزانه** (۲۲ امن از ۲۵ درخواست/کلید) + صف ۱۲ ثانیه‌ای + مدارشکن 429.
- جدول بر اساس نوع دارایی (سهام: Symbol/شرکت/Price/تغییر/باز/بالا/پایین/حجم/۷ روز)؛
  **اطلاعات کامل شرکت** (P/E، EPS، سود تقسیمی، ۵۲ هفته، Beta، درآمد، EBITDA، رشد، هدف تحلیلگران…)
  با باز کردن ردیف — در صورت تقاضا (سهمیه‌بندی).
- بدون کلید سرور → پیام شفاف «پیکربندی نشده» + N/A (هرگز داده جعلی).

### ۱-۴) موتور عملکرد (Market Performance Engine)

- توابع خالص: `getGainers / getLosers / getPerformance` برای هر Universe و بازه ۱H/۲۴H/۷D/۳۰D.
- رتبه‌بندی فقط روی دارایی‌های با داده معتبر؛ «برترین‌ها» فقط رشدکننده‌ها و «بازنده‌ها» فقط افت‌کننده‌ها.
- فرمول: `((قیمت فعلی − قیمت قبلی) / قیمت قبلی) × 100` — از داده Provider.

### ۱-۵) دروازه نرخ سراسری (CoinGecko Global Gate)

در بارگذاری صفحه، چند ماژول (بازار جدید + Top200 قدیمی + توکن‌ایز + لوگوها + جستجوها)
هم‌زمان به CoinGecko درخواست می‌دهند و سهمیه لحظه‌ای IP را تمام می‌کنند → 429 زنجیره‌ای.
راه‌حل: **یک صف سراسری واحد** (`shared/lib/coingeckoGate.ts`) که همه درخواست‌های همه
ماژول‌ها را سریال می‌کند (فاصله ۲۰ ثانیه + مدارشکن ۲ دقیقه‌ای):

- `coingeckoProvider` (ماژول بازار) — از دروازه
- `useTopCryptoMarkets` (Top200 قدیمی) — از دروازه
- `shared/lib/coingecko.ts` (قیمت‌ها/لوگوها/توکن‌ایز) — از دروازه
- `useTopPerformers` و `calculators/historical` — از دروازه

**فالبک اسنپ‌شات کریپتو**: اگر Top200 زنده خطا بخورد، از اسنپ‌شات موفق قبلی
(داده واقعی CoinGecko تا ۷ روز) خوانده و با برچسب «کش» نمایش داده می‌شود — هرگز
صفر/مصنوعی. همگام‌سازی کریپتو اولویت دارد (retry ۶۰ ثانیه) و RWA بعد از آن
(retry ۱۵۰ ثانیه)؛ موفقیت هر اسکن با رویداد `market:*:synced` به UI اعلام می‌شود.

### ۱-۶) معماری داده

```
Market UI → Internal Market API (service/hooks) → Provider Adapter
           → Normalizer (Pure) → Database/Cache (Dexie) → Store → UI
```

- جداول جدید: `marketAssets` · `marketQuotes` · `marketHistorical` · `marketFundamentals` ·
  `marketCategories` · `marketProviderMappings` (Dexie v7).
- هر رکورد `lastFetchedAt / nextRefreshAt / cacheStatus` دارد (قانون ۳۸).
- هویت دارایی بر اساس `provider + providerAssetId (+ contractAddress/chain)` — بدون تکرار (قانون ۳۷).
- پروکسی سرور-سمت: `/coingecko-api` و `/alphavantage-api` (همه درخواست‌ها same-origin؛
  کلید AV در سرور تزریق می‌شود؛ هدرهای مرورگر حذف می‌شوند).

### ۱-۷) حذف کامل Mock/کاتالوگ مرجع از ماژول بازار

- حذف: `MarketDiscoveryPage`، `useMarketDiscovery`، `marketCapReference.ts` (MCAP تقریبی «≈ مرجع»)،
  `classification/underlying` قدیمی، قیمت‌های مرجع `TRADFI_JUL_2026` و `TOKENIZED_STOCK_PRICES`
  از مسیر ماژول بازار.
- **هیچ Asset نمونه‌ای برای نمایش Production باقی نمانده** — همه ردیف‌ها از Provider می‌آیند.
- موبایل: جدول → کارت (با معادل تومانی برای MCap — ارقام فارسی).

---

## ۲) اصلاحیه تحلیل PT (Pendle) — باگ گزارش‌شده کاربر

### باگ ۱ — ارزش بازخرید با قیمت اشتباه (ریشه اعداد −۹۸٪)

**قبل از اصلاح**: `calcPt` ارزش نهایی را `PT مقدار × ۱ دلار` حساب می‌کرد (فرض استیبل‌کوین).
برای بازار **STRCx (xStocks)** که قیمت دارایی پایه ≈ ۹۴٫۵ دلار است، این فرض غلط باعث می‌شد:
سرمایه ۱۰٬۰۰۰$، PT=93.56 → ۱۰۶٫۸۸ PT → ارزش «۱۰۶٫۸۸ دلار» → سود **−۹٬۸۹۳$** (−۹۸٫۹۳٪) ← **کاملاً غلط**.

**بعد از اصلاح**: ارزش بازخرید = `PT مقدار × قیمت واقعی دارایی پایه (از API)`:
۱۰٬۰۰۰$ ÷ 93.56 = ۱۰۶٫۸۸ PT × 94.5 = **≈ ۱۰٬۱۰۰$** → سود **≈ +۱۰۰$ (ROI ≈ +۱٪)** ✔

- اگر قیمت پایه از API در دسترس نباشد: فقط برای استیبل‌ها (USDC/USDT/DAI/…) ۱ دلار فرض می‌شود؛
  برای دارایی غیراستیبل → **«داده ناکافی» (N/A)** — هرگز حدس نمی‌زنیم.
- خروجی شامل «قیمت بازخرید (هر PT در سررسید)» برای شفافیت کامل.

### باگ ۲ — کلید قیمت‌ها در `usePendleMarkets`

فیلدهای `pt/yt/sy/underlyingAsset` از API **خودشان با پیشوند chainId** می‌آیند (مثل `1-0x4ad8…`)؛
کد قبلی دوباره chainId اضافه می‌کرد (`1-1-0x…`) → API مقدار **MISSING** برمی‌گرداند.
اصلاح: فقط `m.pt` / `m.yt` / `m.sy` / `m.underlyingAsset` ارسال می‌شود.

### حذف حدس‌های عددی در تحلیل

- قیمت فرضی `0.948` برای PT حذف شد → بدون قیمت زنده: «داده ناکافی».
- قیمت فرضی `0.05` برای YT حذف شد → قیمت واقعی YT از API؛ وگرنه N/A.
- Break-even هم بر اساس قیمت واقعی PT محاسبه می‌شود (نه 0.948).

---

## ۳) شواهد تست

| مورد | نتیجه |
|---|---|
| تست واحد (`npm run test`) | **۵۸۹ پاس** (۲۸ فایل) — شامل ۳۰ تست جدید ماژول بازار + ۳ تست جدید PT |
| TypeScript (`tsc --noEmit`) | سبز |
| بیلد Production (`npm run build`) | سبز (PWA + Service Worker) |
| تست زنده زنده‌نگه‌داری (Live) | کریپتو: **۲۰۰ ردیف** با قیمت/حجم/MCAP/FDV + اسپارکلاین (تأیید چندباره)؛ RWA: **۲۵۰-۳۵۶ دارایی** با قیمت/حجم/اسپارکلاین واقعی (تأیید چندباره) |
| اسموک مرورگر (`smoke.mjs`) | **۹۱ پاس / ۱ خطا** — تنها خطا «سرمایه فرضی» به نوسان قیمت لحظه‌ای داشبورد وابسته است (نه باگ) |

### تست زنده هر Universe (سندباکس — پنجره‌های سالم)

```
Crypto: 200 ردیف زنده — BTC/ETH با MCap/FDV/نسبت + 427 اسپارکلاین + جستجو SOL ✓
RWA:    250-356 ردیف — Abbott (Ondo Tokenized Stock) $111.44، Mcap $1.5M،
        حجم $171K، اسپارکلاین +3.5%، ستون‌های مستقل Price/Tokenized ✓
TradFi: ردیف‌ها + کارت سهمیه/پیکربندی ✓ (کلید دمو سندباکس محدود است)
```

### تست زنده PT (بازتولید سناریوی کاربر)

```
سرمایه: 10,000 | PT: 93.56 | دارایی پایه: 94.5 | ۱۶ روز تا سررسید
قبل: ارزش سررسید $106.88 | سود -$9,893 | ROI -98.93%  ❌
بعد: ارزش سررسید ≈ $10,100 | سود ≈ +$100 | ROI ≈ +1%   ✅
```

---

## ۴) محدودیت‌های محیط سندباکس (نه باگ اپ)

- **CoinGecko** از IP سندباکس (دیتاسنتر) بسیار محدود است (~۲ درخواست/دقیقه و سقف ساعتی) —
  در محیط کاربری عادی سقف رایگان ۱۰-۳۰ درخواست/دقیقه است. اپ با مدارشکن + کش + اسکن
  پیش‌رونده + برچسب «داده ناکافی» کاملاً مقاوم است.
- **کلید دمو Alpha Vantage** مشترک و عمدتاً مصرف‌شده است — برای داده کامل سنتی،
  `ALPHAVANTAGE_API_KEY=کلید_واقعی` در سرور تنظیم شود.
- در همین سندباکس، پنجره‌های سالم به‌طور زنده تأیید شدند (۲۰۰ ردیف کریپتو + ۳۵۶ RWA با قیمت واقعی).

## ۵) راه‌اندازی

```bash
ALPHAVANTAGE_API_KEY=کلید_شما npm run dev     # کلید فقط سرور-سمت
npm run test                                    # ۵۸۹ تست
node scripts/smoke.mjs                          # اسموک مرورگر
npm run build                                   # بیلد PWA
```
