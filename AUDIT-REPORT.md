# گزارش ممیزی جامع UX/UI و بومیسازی فارسی
## Personal Wealth Operating System — Portfolio & Cross-Asset Simulator PWA

**نسخه گزارش:** 1.0 · **تاریخ:** ۱۴ مرداد ۱۴۰۵ · **دامنه:** UX / UI / Design System / معماری کامپوننت / تایپوگرافی / RTL / دسترسپذیری / PWA
**منبع:** کد واقعی پروژه (شواهد از `src/` استخراج شدهاند) + معیارهای Apple HIG، Material 3، Bloomberg، Linear، Notion، Ramp، Copilot Money، Monarch Money، TradingView

---

## ۰) خلاصه مدیریتی

| حوزه | امتیاز (از ۱۰) | خلاصه وضعیت |
|---|---|---|
| **Design System** | **6.5** | پایه شیشهای منسجم و زیبا؛ اما توکنهای معنایی ناقص، ۲۵ مورد متن ۱۰px و کنتراست ضعیف در حالت روشن |
| **UX** | **6.0** | ناوبری ساده و خواناتر؛ اما بدون Command Palette، کیبورد، حالت خالی/خطا و جریان مالی کامل |
| **معماری Front-End** | **8.0** | قویترین بخش پروژه — Clean Architecture + Feature-Based واقعی و اجراشده |
| **Dashboard** | **4.5** | فقط ۳ کارت؛ یک «سیستم عامل ثروت» به ماژولهای بیشتر نیاز دارد |
| **جداول** | **6.5** | ستونهای الزامی درستاند؛ اما بدون مرتبسازی روی هدر، گروهبندی، خروجی CSV و Drill-Down |
| **نمودارها** | **2.0** | عملاً هیچ نموداری در اپ وجود ندارد (حذف شده)؛ chart.js در dependencies بلااستفاده مانده |
| **PWA** | **6.0** | نصبپذیری و آفلاین خوب؛ بدون Push، Background Sync، Splash سفارشی و UX بهروزرسانی |
| **RTL / فارسی** | **6.5** | چیدمان RTL عالی؛ اما ایزولهسازی LTR دادههای مالی اجرا نشده و ارقام فارسیاند |
| **تایپوگرافی** | **7.5** | انتخاب Vazirmatn درست است؛ با جایگزین قوی Estedad برای دادههای متراکم |
| **دسترسپذیری** | **4.5** | شکافهای واقعی: فوکوس، کیبورد، Screen Reader، کنتراست حالت روشن |
| **موبایل** | **7.0** | iOS Standalone آماده؛ تبلت/فولد/دسکتاپ غفلت شدهاند |
| **امتیاز کل** | **6.2/10** | **«پروتوتایپ قوی → محصول Enterprise»: شکاف اصلی در دادههای مالی، نمودار، دسترسپذیری و LTR Isolation است** |

**راهبرد پیشنهادی:** این پروژه از نظر معماری و بنیان UI در نیمه بالایی قرار دارد؛ مسیر رسیدن به کلاس جهانی ۳ محور دارد: **(۱) استانداردسازی نمایش دادههای مالی (LTR/Latin + جدول fx) (۲) بازسازی سطوح داده (جداول + نمودار + Drill-Down) (۳) تکمیل دسترسپذیری و کیبورد.** جزئیات و نقشه راه در بخش ۱۶.

---

## ۱) ممیزی Design System

| زیرحوزه | امتیاز | نکته کلیدی |
|---|---|---|
| Typography | 7.0 | Vazirmatn باندلشده؛ اما سلسلهمراتب مقیاس فونت تعریفنشده (۲۵ مورد `text-[10px]` و `text-[9px]` پراکنده) |
| Colors | 6.5 | توکنهای RGB معنایی خوب؛ اما کنتراست حالت روشن در accent/positive زیر AA است (محاسبه واقعی: 3.74 و 3.77) |
| Radius | 8.0 | زبان فرم منسجم (rounded-2xl/3xl/4xl) — استاندارد Material |
| Elevation | 7.0 | طبقهبندی glass / glass-strong / glass-soft خوب |
| Shadows | 6.5 | فقط ۴ سایه؛ سلسلهمراتب ارتفاع (۰/۱/۲/۳) وجود ندارد |
| Motion | 7.5 | Framer Motion + کاهش حرکت `prefers-reduced-motion` پیاده شده (نقطه قوت واقعی) |
| Iconography | 7.0 | Lucide یکدست؛ اما اندازه/وزن/حالتهای آیکون استیلسازیشده نیست |
| Spacing | 7.0 | فواصل نسبی منظم؛ مقیاس spacing توکنی (۴/۸/۱۲/۱۶/۲۴) مستند نشده |
| Grid | 6.0 | grid-cols-2 ثابت؛ بدون Breakpoint برای تبلت/دسکتاپ (max-w-lg یعنی اپ در آیپد شبیه «اپ تلفن زومشده» است) |
| Responsive System | 6.5 | موبایل کامل؛ تبلت/دسکتاپ هیچ استراتژی ندارد |

### یافتههای اصلی

1. **توکنهای معنایی ناقص:** رنگها بهصورت RGB متغیر تعریف شدهاند (عالی)، اما توکنهای `text-muted` در حالت روشن روی شیشهی شفاف + گرادیان پسزمینه، عملاً زیر ۴٫۵:۱ سقوط میکنند. **شواهد محاسبهشده:** `accent #0d9488` روی سفید = **3.74:1** (FAIL برای متن عادی)، `positive #059669` = **3.77:1** (FAIL). حالت تیره همهچیز پاس است ✓.
2. **مقیاس تایپ تعریفنشده:** برچسبها از ۹px تا ۱۳px بدون سیستم. ۹-۱۰px در هیچ HIG اپل/گوگل پذیرفته نیست (حداقل توصیهشده: ۱۲px).
3. **Elevation فاقد سیستم:** بهجای ۳-۴ لایه ارتفاع مستند، ترکیبهای آزاد blur/shadow.

### اقدامات پیشنهادی (P1)
- تعریف `type-scale` با ۷ سطح (display/headline/title/body/label/caption/tabular) و ممنوعیت زیر ۱۲px.
- تیرهکردن accent حالت روشن به نسخه AA (`#0f766e` = 4.6:1) و positive به `#047857` (4.5:1) یا استفاده از وزن/زمینه جبرانی.
- ساخت توکن elevation با ۴ سطح (`e0` تا `e3`) و فایل `tokens.ts` واحد بهجای کلاسهای پراکنده.

---

## ۲) ممیزی UX

| زیرحوزه | امتیاز | وضعیت |
|---|---|---|
| Navigation | 7.5 | ۳ مقصد اصلی + BottomNav با safe-area و شاخص حرکت — الگوی صحیح iOS |
| Information Architecture | 6.5 | داشبورد/شبیهسازی/بازار منطقی؛ اما «سناریو» و «تنظیمات» در شیت مدفوناند |
| Discoverability | 6.0 | اکشنهای اصلی (رفرش، سناریو) قابل کشفاند؛ قابلیتهای «حذفشده» (نمودار، export) اصلاً وجود ندارند |
| Learnability | 6.5 | مفاهیم «سناریوی مستقل» و «اسنپشات» با نوارهای توضیحی خوب روشن شدهاند؛ اما اصطلاحات فنی بدون Glossaryاند |
| Cognitive Load | 5.5 | جدول ۲۲۶ ردیفی بدون خلاصه عددی بالای آن، کاربر را غرق میکند؛ کارتهای تحلیلی جایگزین خوبیاند اما کافی نیستند |
| Empty States | 5.0 | برای جستجو وجود دارد؛ برای «بدون داده/آفلاین کامل» طراحی نشده |
| Error States | 5.5 | خطاها بیصدا به فالبک میروند؛ هیچ Toast/هشدار برای کاربر (مثل «API محدود شد») وجود ندارد |
| Loading States | 7.0 | Skeleton + نوار پیشرفت سهام + همگامسازی توکنایز — خوب |
| Search Experience | 6.5 | جستجوی ساده با نرمالسازی فارسی؛ بدون فیلتر ترکیبی، history یا highlight |
| Command Palette | **0** | وجود ندارد (الزامی برای کلاس Linear/Ramp) |
| Keyboard Navigation | **3.0** | فقط Tab پیشفرض؛ بدون میانبر، بدون ESC در شیتها (شواهد: `grep Escape` = 0)، بدون فلش در جدول |
| Data Entry | 6.5 | RHF+Zod قوی؛ اما بدون اعتبارسنجی بلادرنگ فرمت ارزی، بدون undo برای ذخیره سناریو |
| Financial Workflow | 5.5 | جریان «مشاهده → تحلیل → تصمیم» ناقص است: بعد از دیدن ردیف، هیچ «خرید/جزئیات/مقایسه» وجود ندارد |

### یافتههای کلیدی
- **گمگشتگی معنایی پس از حذف کارتهای آماری:** کاربر با جدول بزرگ تنها میماند؛ کارتهای تحلیلی (بیشترین/کمترین رشد) شروع خوبیاند ولی باید با یک **خلاصه عددی کوچک** (میانگین بازده، تعداد داراییهای سودده/ضررده) همراه شوند — بدون بازگشت به «ارزش پرتفوی جمعشده» که حذف آن درست بود.
- **هیچ راهی برای «واکنش» به دادهها نیست:** ردیفهای جدول غیرقابل کلیکاند؛ در کلاس Monarch/Copilot هر ردیف → جزئیات + نمودار + هشدار.

---

## ۳) معماری Front-End — قویترین بخش (8.0/10)

| معیار | امتیاز | ارزیابی |
|---|---|---|
| Component Structure | 8.5 | feature-based + shared/ui واقعی و تمیز |
| Reusability | 8.0 | GlassCard/Button/Sheet/SourceBadge/AssetLogo واقعاً reusable |
| Composition | 8.0 | ترکیب کامپوننتها بدون props-drilling عمیق |
| Atomic Design | 7.5 | بدون نامگذاری اتم/مولکول ولی ساختار معادل آن رعایت شده |
| State Management | 8.5 | Zustand برای UI-state + TanStack برای server-state — تقسیم درست؛ single-flight برای چرخه سهام عالی |
| Folder Structure | 8.5 | `domain/data/presentation` در هر feature با دامنهی خالص (engine بدون React) |
| Scalability | 7.5 | قابل رشد؛ دو ریسک: `tokenizedCoinMap.ts` (۲۶KB seed) در لایه data وابسته به constants دامنه است؛ hook های shared باید به `features/shared` مهاجرت کنند |

**نکات ریز:**
- `useAssetMeta` در `shared/hooks` قرار دارد ولی منطق کوینگکو را دارد — مرز لایهها را کمی شل میکند؛ انتقال به `features/market` یا `shared/lib` تمیزتر است.
- `RateLimitedQueue` (صف + مدارشکن) از نظر طراحی کلاس جهانی است — در بخش PWA به آن ارجاع دوباره میشود.
- تست خودکار وجود ندارد (فقط smoke script دستی). برای Enterprise: Vitest + Testing Library برای engine و کامپوننتهای حیاتی.

---

## ۴) داشبورد (4.5/10)

وضعیت فعلی: کارت اتریوم + ۲ کارت ورود به بازه + ۳ چیپ قابلیت.

| ماژول مورد انتظار | وضعیت | پیشنهاد |
|---|---|---|
| Financial Summary (ارزش خالص) | ⚠️ جزئی | فقط ETH؛ باید + USDT + مجموع بهتفکیک ارز با فرمت LTR |
| Portfolio | ⚠️ جزئی | کارت ETH خوب؛ بدون توزیع وزنی (ETH 28.8% / USDT 71.2%) |
| Cash Flow | ✗ | برای این محصول «موجودی و تخصیص» کافی است؛ ویجت USDC/USDT |
| Risk | ✗ | تلویحاً در کارتهای تحلیلی (بیشترین ضرر)؛ پیشنهاد: نشانگر نوسان و «عمق ضرر» |
| Market Overview | ⚠️ | صفحه بازار جداگانه هست ولی در داشبورد خلاصه بازار (۳-۵ دارایی پربیننده) نیست |
| Watchlists | ✗ | وجود ندارد؛ ستون «مشاهدهشده/ستارهدار» در Dexie کمهزینه است (جدول `watchlist`) |
| FX ویجت | ✗ | طبق بخش ۱۱، ویجت دلار/تومان با تاریخچه ۲۴ ساعته |

**طراحی پیشنهادی صفحه اول (بهترتیب):**
1. **Hero Net Worth:** مجموع واقعی (ETH×قیمت + USDT) با ارقام LTR + معادل تومانی + تغییر ۲۴ساعته
2. **کارت اتریوم** (موجود)
3. **توزیع دارایی** (نوار پیشرفت ساده — نه نمودار حجیم)
4. **خلاصه دو بازه** (موجود، ارتقا)
5. **Watchlist + خلاصه بازار**

---

## ۵) جداول (6.5/10)

| معیار | امتیاز | وضعیت |
|---|---|---|
| Data Density | 7.0 | تراکم خوب؛ لوگو+نماد+نام فارسی در ستون چسبان |
| Sorting | 5.0 | فقط select بالای جدول؛ هدرها غیرقابل کلیک، بدون نشانگر جهت |
| Filtering | 6.5 | چیپهای دسته + جستجو؛ بدون فیلتر ترکیبی چندگانه/فیلتر دامنه قیمت |
| Grouping | 4.0 | هیچ گروهبندی بصری (رمز/توکنایز/سنتی) در بدنه جدول نیست — فقط فیلتر |
| Virtualization | 8.0 | برای ۲۲۶ ردیف لازم نیست؛ تصمیم درست. اما اگر کاتالوگ به ۱۰۰۰+ برسد، react-window ضروری میشود |
| Sticky Columns | 6.5 | ستون اول چسبان است ولی پسزمینه `bg-card/95` نیمهشفاف → متن از زیر ردیفها میگذرد. باید ۱۰۰٪ مات شود |
| Export | **0** | هیچ خروجی CSV/Excel وجود ندارد (شواهد: بدون Blob/toCsv در کد) |
| Drill Down | **0** | ردیفها غیرقابل کلیکاند |

**اقدامات P0/P1:**
- مرتبسازی با کلیک روی هدر (با نشانگر ▲▼) + حفظ فرمولهای ستونها
- گروهبندی اختیاری: ردیفهای «رمزنگاری / توکنایز / سنتی» با جمع کوچک هر گروه
- دکمه **Export CSV** (کمترین کد: ساخت Blob از `rows`)
- **Drill-Down:** کلیک روی ردیف → شیت جزئیات دارایی (قیمت، بازده، مقایسه با ETH، نمودار کوچک ۷ روزه، دکمه افزودن به Watchlist)
- `bg` مات برای ستون چسبان + سایهی لبهی اسکرول (gradient در لبه سمت راست)

---

## ۶) نمودارها (2.0/10)

**وضعیت:** تنها نمودار (Doughnut تخصیص) در بهروزرسانی قبلی طبق دستور حذف شد؛ chart.js همچنان در dependencies است ولی هیچ استفادهی فعالی ندارد.

| معیار | ارزیابی |
|---|---|
| انتخاب کتابخانه | chart.js (حاضر) سبک و کافی برای شروع؛ برای کندل/هیتمپ مالی بعداً ApexCharts یا lightweight-charts (TradingView) |
| نوع نمودارها | پیشنهاد ۴ نمودار: (۱) خط عملکرد بازه — سبد توزین برابر vs معیار ETH vs بهترین دارایی (۲) دونات تخصیص با لِجند خوانا (۳) خط ۲۴ساعته نرخ FX (۴) هیستوگرام توزیع بازده |
| خوانایی | اجباری: tooltip با ارقام LTR ایزوله + `rtl: true` در Chart.js (پشتیبانی میشود) |
| تعامل | کلیک روی بخش نمودار → فیلتر جدول؛ هاور → tooltip |
| Performance | chart.js با lazy mount (فقط وقتی در viewport) |

**توجه:** کاربر طبق دستور قبلی نمودار دستهبندی را حذف خواست — نمودارهای پیشنهادی (عملکرد بازه، FX) ماهیت متفاوتی دارند و با دستور قبلی تناقض ندارند؛ اما **فقط پس از تأیید شما** اضافه شوند.

---

## ۷) PWA (6.0/10)

| معیار | امتیاز | وضعیت |
|---|---|---|
| Installability | 8.0 | manifest + beforeinstallprompt سفارشی + راهنمای iOS — کامل |
| Offline | 7.5 | precache کامل + NetworkFirst کوینگکو + فالبک اسنپشات — قوی |
| Service Worker | 7.0 | Workbox generateSW درست؛ اما بدون آپدیت-نوتیفیکیشن (autoUpdate بیسروصدا) |
| Manifest | 8.0 | کامل + maskable icon |
| Splash Screen | 4.0 | Android از background_color استفاده میکند؛ iOS هیچ تصویر راهانداز ندارد (`apple-touch-startup-image` صفر) |
| Push Notification | **0** | وجود ندارد (برای هشدار قیمت کاربردی است) |
| Background Sync | **0** | وجود ندارد (برای «همگامسازی قیمتها هنگام اتصال مجدد» مفید است) |
| Cache Strategy | 7.5 | کوینگکو NetworkFirst ✓، آلفاوانتج از کش IndexedDB ✓؛ کش CDN لوگوها تعریف نشده |

**پیشنهادها:** toast «نسخه جدید آماده است» (onNeedRefresh)؛ pre-cache دامنهی `coin-images.coingecko.com` و `assets.parqet.com` با CacheFirst (عمر ۳۰ روز) برای لوگوها؛ افزودن `periodicBackgroundSync` برای رفرش قیمتها در PWA نصبشده.

---

## ۸) RTL / فارسی (6.5/10) — محور استراتژیک

### وضعیت فعلی (شواهد کد)
- چیدمان، متن، منو و فرمها RTL کامل ✓
- **ارقام:** همهجا `fa-IR` (۶ مورد) — اعداد فارسی در ستونهای مالی
- **ایزولهسازی LTR:** فقط ۱ مورد `dir="ltr"` در کل کد (فیلد کلید API)
- جداول دارای `border-spacing` و `start-0`/`ps-10` (منطقی RTL) ✓ — اینها درستاند

### قانون طلایی دادههای مالی (الزامی)
چیدمان RTL بماند، اما **هر مقدار مالی باید LTR-ایزوله با ارقام لاتین باشد:**

| عنصر | نمایش صحیح | چرا |
|---|---|---|
| قیمت/ارزش | `$36,900.00` | ترتیب `$` و ارقام در RTL برعکس دیده میشود؛ ایزولهی LTR ترتیب را تثبیت میکند |
| درصد | `+2.41%` | علامت مثبت/درصد در متن RTL جا به جا میشوند |
| سود/زیان | `+$4,537.31` یا `−$784.36` | منفی باید `−` (منفی یونیکد) باشد، نه پرانتز (پرانتزها در BiDi جابهجا میشوند) |
| معادل تومانی | `≈ 5.46 میلیارد تومان` | عدد لاتین LTR، واحد فارسی — ترکیب امن |
| تاریخ | `۱ ژانویه ۲۰۲۵` (تقویم میلادی) یا جلالی | ثبات در کل اپ؛ پیشنهاد: جلالی برای UI، میلادی برای داده |
| نماد سهام | `NVDAB` | LTR در سلول |

### پیادهسازی فنی پیشنهادی
```css
/* utility سیستماتیک */
.num-ltr { direction: ltr; unicode-bidi: isolate; font-variant-numeric: tabular-nums; }
```
- هر سلول عددی: `<span class="num-ltr">$36,900.00</span>` — **isolate** از نشت جهت به متن اطراف جلوگیری میکند.
- فرمترها: ارقام لاتین (`en-US`) برای همه مقادیر مالی؛ ارقام فارسی فقط در متن روایی (مثل «۲۲۶ ردیف»).
- جدول: `text-align` بر اساس نوع ستون (ستون دارایی right، اعداد left در RTL) — یا بهتر: اعداد با `text-start` و جداکنندهی راستچین در هدر.

### BiDi Checklist برای هر کامپوننت جدید
- [ ] مقادیر مالی داخل `dir="ltr"` یا `.num-ltr`
- [ ] علامت منفی یونیکد `−` بهجای `-` در انتهای عدد
- [ ] تست چشم: ترکیب «نام فارسی + قیمت» در یک جمله شکسته نمیشود
- [ ] Chart.js با `rtl: true` و tooltip LTR
- [ ] هیچ `text-left/right` فیزیکی — همیشه `start/end`

---

## ۹) تایپوگرافی فارسی — مقایسه ۵ فونت

| معیار | **Vazirmatn** (فعال) | **Estedad** | **IRANSans** | **Yekan Bakh** | **Shabnam** |
|---|---|---|---|---|---|
| مجوز | OFL ✓ | OFL ✓ | اختصاصی (محدود) ⚠️ | نامشخص ⚠️ | OFL ✓ |
| پشتیبانی variable/webfont | عالی (Fontsource) | عالی | متوسط | متوسط | ضعیف |
| ارتفاع x / خوانایی صفحه | خوب | **عالی (طراحیشده برای UI)** | متوسط (قدیمی) | خوب (نمایشی) | پایینتر |
| ارقام | متناسب + نسخه mono | متناسب؛ tabular خوب | معمولی | معمولی | معمولی |
| مناسب داده متراکم (جدول/داشبورد) | خوب | **بسیار خوب** | متوسط | ضعیف | متوسط |
| پشتیبانی وزنها | ۴۰۰–۹۰۰ کامل | ۱۰۰–۹۰۰ کامل | ۳ وزن | ۴-۵ وزن | ۳ وزن |
| نگهداری فعال | ✓ فعال | ✓ فعال | ✗ تقریباً متوقف | ✗ | نیمهفعال |
| ریسک | بسیار کم | کم | **بالا (قانونی)** | متوسط | کم |

### نتیجهگیری
- **توصیه اصلی: ادامه با Vazirmatn** — اکوسیستم کامل (Fontsource + variable)، مجوز آزاد، خوانایی عالی، و ریسک صفر. انتخاب فعلی درست بوده است ✓
- **جایگزین برتر برای دادههای متراکم: Estedad** — اگر در آینده جدولها سنگینتر شدند (و کارفرما بخواهد)، مهاجرت کماستهزینه است (هر دو OFL و ساختار متریک مشابه).
- **هرگز در پروژه Enterprise:** IRANSans (مجوز) و Yekan Bakh (مجوز مبهم).
- **الزام فنی:** با هر دو فونت، برای اعداد مالی `font-variant-numeric: tabular-nums` و ارقام لاتین — در Vazirmatn ارقام فارسی متناسباند و در ستونهای عددی ردیفها میلرزند؛ مشکل با ارقام لاتین + tnum حل میشود.

---

## ۱۰) قالببندی اعداد و دادههای مالی + طراحی جدول fx_rates

### قوانین قطعی نمایش
| سناریو | فرمت | نمونه |
|---|---|---|
| ارزش خالص دارایی (دلار) | `$` + ارقام لاتین + جداکننده کاما + ۲ اعشار | `$36,900.00` |
| معادل تومانی (ثانویه) | `≈` + عدد لاتین + «میلیارد تومان» | `≈ 5.46 میلیارد تومان` |
| تغییر | علامت صریح + درصد + فلش جهت | `+2.41% ▲` / `−1.20% ▼` |
| سود/زیان | علامت + `$` + ۲ اعشار | `+$4,537.31` |
| تعداد ردیف | ارقام فارسی (متن روایی) | `۲۲۶ ردیف` |
| نسبت/وزن | درصد با ۱ اعشار | `71.2%` |

**رنگ بهتنهایی کافی نیست** — همیشه علامت (▲▼ / +−) همراه رنگ (شرط WCAP برای کوررنگی).

### طراحی جدول `fx_rates` (پیشنهاد معماری)
```
Dexie: fx_rates
{
  id: string            // 'usd-irr' یا date-based
  rate: number          // نرخ دلار به تومان (مثلاً 1_482_000)
  source: 'admin' | 'auto'
  updatedAt: number     // timestamp
  history: Array<{ t: number; rate: number }>  // ۲۴ ساعت گذشته (prune روی insert)
}
```
- **ذخیره:** هر آپدیت دستی ادمین → رکورد جدید با `t`؛ نگهداری حداکثر ۲۴ ساعت (پاکسازی خودکار رکوردهای قدیمیتر).
- **UI ادمین (تنظیمات):** فیلد عددی + دکمه «ثبت نرخ» + نمودار کوچک ۲۴ساعته + برچسب «آخرین بهروزرسانی: ۱۴:۰۵».
- **استفاده:** کامپوننت `TomanEquivalent` که `$` را با نرخ جاری ضرب و با `Intl` تومان قالببندی میکند (ایزوله LTR).
- **آفلاین:** نرخ آخر در IndexedDB؛ هنگام آنلاین، ادمین بهروزرسانی میکند (بدون اتوماتیک — طبق نیازمندی «قابل آپدیت دستی توسط ادمین»).

---

## ۱۱) دسترسپذیری (4.5/10) — شواهد واقعی

| معیار | وضعیت |
|---|---|
| Contrast | ⚠️ حالت روشن: accent 3.74:1 و positive 3.77:1 (FAIL AA برای متن عادی)؛ تیره: همه پاس ✓ |
| Font Size | ✗ ۲۵ مورد `text-[10px]`/`text-[9px]` در کد |
| Keyboard | ✗ بدون ESC در Sheet (0 مورد)، بدون skip-link (0)، بدون میانبر |
| Screen Reader | ✗ جدول بدون `aria-sort`/`scope` (0 مورد)؛ بهروزرسانی قیمتها بدون `aria-live` کنترلشده |
| Focus Ring | ✗ فقط ۱ استایل فوکوس سفارشی؛ بقیه به پیشفرض مرورگر وابستهاند |
| Color Blindness | ⚠️ P&L فقط رنگ (سبز/قرمز) — بدون علامت ▲▼ در همهجا |
| Motion Reduction | ✓ `prefers-reduced-motion` پیاده شده — این را حفظ کنید |

### اقدامات P0
1. `:focus-visible` سراسری با حلقه ۲px accent + offset
2. حذف زیر ۱۲px؛ caption 12px حداقل
3. جدول: `aria-sort` روی هدرها، `scope="col"`، `<caption class="sr-only">`
4. Sheet: ESC + فوکوس trap + بازگرداندن فوکوس به دکمه مبدأ
5. رنگ + نماد برای همه حالتهای مثبت/منفی (نیازمند استایلهای فعلی است که فقط رنگ دارند)
6. `aria-live="polite"` با throttling برای تغییرات قیمت (نه برای هر ردیف!)

---

## ۱۲) موبایل و پاسخگویی (7.0/10)

| پلتفرم | وضعیت |
|---|---|
| iPhone (پشتیبانیشده) | ✓ safe-area، standalone، viewport-fit، status bar |
| Android | ✓ (تست واقعی نیاز است) |
| Foldable (مثل Z Fold) | ✗ اپ در حالت باز وسط صفحه میماند؛ نیاز به رسانهی `(min-width: 700px)` و دوستونهشدن |
| Tablet/iPad | ✗ `max-w-lg` (۵۱۲px) یعنی iPad = اپ تلفن زومشده؛ انتظار چیدمان تطبیقی |
| Desktop | ✗ فقط مرورگر موبایل؛ برای یک «سیستم عامل ثروت» دسکتاپ هم لازم است |

**پیشنهاد:** رسانههای تدریجی: موبایل (پایه) → `≥640px` (تبلت، ۲ ستون کارتها) → `≥1024px` (دسکتاپ، سایدبار + جدول تمامعرض + چندستونه). جدول تمامعرض در دسکتاپ میتواند ستونهای بیشتری (بازده، منبع، لوگو) نشان دهد (progressive disclosure).

---

## ۱۳) نقد بیرحمانه (Design Critique)

| استاندارد مرجع | شکاف فعلی | شدت |
|---|---|---|
| **Bloomberg Terminal** | تراکم داده و کیبورد-فرست بودن: جدول ما با ۲۲۶ ردیف بهجای بهرهوری، «اسکرول بینهایت» دارد؛ هیچ جریان کیبوردی (فلش → ردیف → جزئیات) نیست | بالا |
| **Linear** | Command Palette (`⌘K`) و جستجوی جهانی غایب است؛ جابهجایی بین صفحات فقط از نوار پایین است | بالا |
| **Apple HIG** | اسپلش iOS، فیدبک لمسی (haptic)، ژستهای استاندارد (pull-to-refresh در جدول) نداریم | متوسط |
| **Material 3** | حالتهای تعامل (pressed/focus/disabled) در Button از قبل هست ولی در ردیفهای جدول/کارتها ناقص است؛ فونت ۹px نقض M3 | بالا |
| **TradingView** | بدون نمودار، بدون watchlist، بدون مقایسه همزمان چند دارایی | بالا |
| **Monarch / Copilot Money** | «داشبورد ثروت» واقعی نیست: Net Worth، توزیع، جریان، اهداف — نداریم؛ دادهها در جدول خام رها شدهاند | بالا |
| **Ramp** | خروجی داده (CSV/API)، جستجوی سریع، و «اعمال» روی دادهها (هشدار، پیگیری) غایب است | متوسط |
| **Notion** | خالیها و صفحات خوشآمدگویی/راهنما نداریم؛ کاربر جدید بدون راهنما در مفهوم «اسنپشات» گم میشود | متوسط |

**نقدهای ریز و بیپرده:**
1. **«شیشه برای نمایش، نه برای داده»:** glassmorphism روی بدنهها قشنگ است، اما وقتی هر سلول جدول روی پسزمینهی شناور میخوابد، خوانایی داده ضربه میخورد. قاعده: **سطوح داده باید مات (یا حداکثر ۹۵٪) باشند؛ شیشه فقط برای کارتهای خلاصه.**
2. **اعداد فارسی در ستونهای مالی:** در نگاه اول «بومی» به نظر میرسد، اما در یک داشبورد مالی استاندارد جهانی (و طبق معیار خود شما در بخش ۱۰) **شکست BiDi** است — ارقام لاتین LTR-ایزوله. این بزرگترین تغییر نمایشی لازم است.
3. **کارتهای تحلیلی خوباند ولی «خودستایند» نیستند:** عدد بزرگ بازده خام (مثل ۸۲۷٪) بدون زمینه (volatility, بازده میانه) گمراهکننده است — یک خط «میانه بازده سبد: X٪» ارزش افزودهی واقعی دارد.
4. **تنظیمات دو ورودی دارد** (آیکون TopBar و دکمه سناریو) — نقض «یک راه درست برای هر کار».
5. **حالت خالی جدول فقط یک آیکون دارد** — در حالت «همه N/A» (آفلاین کامل) باید یک Empty State کامل با راهنمای «اتصال مجدد» بیاید.
6. **هیچ undo/confirmation برای «پاک کردن کش» نیست** — اکشن مخرب بدون تأیید، در استاندارد Enterprise مردود است.
7. **نامگذاری «شبیهسازی»** برای کاربر عادی مبهم است؛ «مقایسه عملکرد» یا «بازده تاریخی» واضحتر است (IA).

---

## ۱۴) امتیازدهی نهایی

| حوزه | امتیاز | رتبه |
|---|---|---|
| Design System | 6.5 | نیازمند تثبیت توکنها |
| UX | 6.0 | نیازمند کیبورد/پالت/خلاصهها |
| Front-End Architecture | 8.0 | کلاس بالا ✓ |
| Dashboard | 4.5 | بازطراحی P0 |
| Tables | 6.5 | ارتقای تعاملی P1 |
| Charts | 2.0 | بازسازی با تأیید کاربر |
| PWA | 6.0 | تکمیل P2 |
| RTL/Persian | 6.5 | LTR Isolation P0 |
| Accessibility | 4.5 | اصلاحات P0 |
| Mobile | 7.0 | تطبیق تبلت/دسکتاپ P2 |
| **کل** | **6.2/10** | |

---

## ۱۵) نقشه راه اجرا (Priority Roadmap)

### P0 — فوری (هستهی محصول مالی)
1. **LTR Isolation + ارقام لاتین در همه مقادیر مالی** (utility `.num-ltr` + فرمترها) — با حفظ متن فارسی روایی
2. **اصلاح کنتراست حالت روشن** (accent/positive → نسخههای AA) و ماتکردن سطوح داده
3. **دسترسپذیری پایه:** focus-visible سراسری، ESC در Sheet، `aria-sort`/`scope` جدول، حذف فونتهای زیر ۱۲px
4. **جدول `fx_rates`** + کامپوننت معادل تومانی + فرم ادمین (طبق بخش ۱۰)

### P1 — مهم (تجربه کاربری کلاس Enterprise)
5. مرتبسازی روی هدر + گروهبندی دستهها + **Export CSV**
6. **Drill-Down:** شیت جزئیات دارایی + Watchlist (جدول Dexie)
7. Command Palette ساده (جستجوی دارایی با `Ctrl/⌘K` → پرش به ردیف)
8. Toast/Error states (محدودیت API، موفقیت ذخیره، آپدیت PWA)
9. خلاصهی عددی بالای جدول (بدون بازگشت به جمع پرتفوی): میانه بازده، تعداد سودده/ضررده، بهترین/بدترین

### P2 — آینده
10. نمودارها (با تأیید کاربر): عملکرد بازه vs ETH + FX ۲۴h
11. Push Notification + Background Sync برای هشدار قیمت
12. چیدمان تطبیقی تبلت/دسکتاپ + Splash iOS
13. تستهای خودکار (Vitest) برای `engine.ts` و کامپوننتهای مالی

---

*این گزارش بر پایهی کد فعلی (revision: vite 6.4.3, react-router 7.18.2) تهیه شده و امتیازها مبتنی بر شواهد قابل استناد است.*
